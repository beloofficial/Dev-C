#include<iostream>
#include<string>
using namespace std;
int main()
{int i,n,a[11111],b[11111],q,h,c[11111];
    string s,j;
    cin>>n;
    for(i=1;i<=n;i++)
    {                
                     cin>>s>>j;
    }
//---------------------------------------------------------------------------------------    
    for(i=1;i<=n;i++)
    {
               for(i=0;i<s.size();i++)
               {
                                                            a[i]=s[i]-48; 
               }
               for(i=0;i<j.size();i++)
               {                  
                                                            b[i]=j[i]-48;
                       
               }
//-------------------------------------------------------------------------    
for(i=0;i<s.size();i++)
{
      for(q=i+1;q<s.size();q++)
      {
              if(a[i]==a[q]&&a[i]!=-1&&a[q]!=-1){a[q]=-1;}                 
      }    
}
sort(a+0,a+s.size()+0); for(i=0;i<s.size();i++)
                       {
                       cout<<a[i]<<" ";
                       }
                       
//---------------------------------------------------------------------------
for(i=0;i<j.size();i++)
{
      for(h=i+1;h<j.size();h++)
      {
              if(b[i]==b[h]&&b[i]!=-1&&b[h]!=-1)b[h]=-1;                 
      }
}    
sort(b+0,b+s.size()+0);for(i=0;i<j.size();i++)
                       {
                       cout<<b[i]<<" ";
                       }

//------------------------------------------------------------------
}                      
system("pause");
return 0;    
}
